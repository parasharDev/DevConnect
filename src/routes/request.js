const express = require("express");
const router = express.Router();


//4. Send Connection Request
router.post("/sendConnectionRequest", async (req, res, next) => {
  try {
    const user = req;
  } catch (err) {}
});

module.exports = router;
